# Uncomment the next line to define a global platform for your project
platform :ios, '16.2'

target 'DogIdentifier' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DogIdentifier

  target 'DogIdentifierTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DogIdentifierUITests' do
    # Pods for testing
  end

pod 'Alamofire', '~>4.4'
pod 'SwiftyJSON'
pod 'SDWebImage'

end
